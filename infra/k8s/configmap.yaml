---
apiVersion: v1
kind: ConfigMap
metadata:
  name: sre-app-config
  namespace: default
  labels:
    app: sre-app
    team: sre
    managed-by: kubectl
data:
  APP_VERSION: "1.0.0"
  ENVIRONMENT: "staging"
  LOG_LEVEL: "INFO"
  PORT: "8080"
